interface:
  display_name: "Execute Plan"
  short_description: "Execute every plan item to verified completion"
  default_prompt: "Use $execute-plan [plan text or path] to preserve the originating requirement verbatim, account for every planned, adapted, and discovered in-scope task, and execute the plan through verified completion or report an explicit incomplete outcome."
