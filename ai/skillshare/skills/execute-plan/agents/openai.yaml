interface:
  display_name: "Execute Plan"
  short_description: "Execute the full plan; default to one autonomous session"
  default_prompt: "Use $execute-plan to complete the full plan, including discovered issues, code-review-loop then final-pass at each required checkpoint, and required checks, in one autonomous session unless the plan explicitly specifies otherwise; respect concrete blockers and my instructions."
