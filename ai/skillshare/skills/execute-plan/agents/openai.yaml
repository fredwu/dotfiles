interface:
  display_name: "Execute Plan"
  short_description: "Persist through every plan item and quality gate"
  default_prompt: "Use $execute-plan [plan text or path] to execute every item and gate to verified completion, continuing automatically across phases without a handoff; report an incomplete outcome only for evidenced genuine blockers after all unaffected work is exhausted."
