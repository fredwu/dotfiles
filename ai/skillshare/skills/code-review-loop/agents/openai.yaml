interface:
  display_name: "Code Review Loop"
  short_description: "Review, remediate, and verify bounded rounds"
  default_prompt: "Use $code-review-loop to review and remediate the current change through bounded rounds."
policy:
  allow_implicit_invocation: true
