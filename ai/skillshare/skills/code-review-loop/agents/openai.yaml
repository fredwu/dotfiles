interface:
  display_name: "Code Review Loop"
  short_description: "Iterate with parallel code review"
  default_prompt: "Use $code-review-loop to review and safely remediate this change until it is clean or bounded."
