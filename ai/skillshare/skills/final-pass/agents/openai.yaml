interface:
  display_name: "Final Pass"
  short_description: "Resolve phase or execution residuals and verify completion"
  default_prompt: "Use $final-pass to resolve all task-relevant omissions and residual issues, then verify this phase or the full execution with applicable checks."
