interface:
  display_name: "Code Review"
  short_description: "Run one read-only internal code review"
  default_prompt: "Use $code-review to review the current change once without modifying it."
policy:
  allow_implicit_invocation: true
