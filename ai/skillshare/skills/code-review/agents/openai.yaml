interface:
  display_name: "Code Review"
  short_description: "Review code changes with parallel confidence checks"
  default_prompt: "Use $code-review to review the current change and report only high-confidence findings."
