interface:
  display_name: "Code Review"
  short_description: "Review pull requests with parallel confidence checks"
  default_prompt: "Use $code-review to review this pull request and post only high-confidence findings."
