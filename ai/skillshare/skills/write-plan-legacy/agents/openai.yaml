interface:
  display_name: "Write Legacy-Removal Plan"
  short_description: "Plan a focused FeedBun legacy-code purge"
  default_prompt: "Use $write-plan-legacy to audit FeedBun-owned code and write an evidence-backed legacy-removal plan."
