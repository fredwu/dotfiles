interface:
  display_name: "Revise Plan"
  short_description: "Revise plan prose without update history"
  default_prompt: "Use $revise-plan to apply the latest $compare-plans findings and guidance throughout my plan while preserving only the original verbatim requirement."
policy:
  allow_implicit_invocation: false
