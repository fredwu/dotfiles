interface:
  display_name: "Revise Plan"
  short_description: "Apply all findings with selective guidance"
  default_prompt: "Use $revise-plan to revise the current plan from every finding in the latest comparison, apply my guidance, and preserve existing verbatim requirement history."
policy:
  allow_implicit_invocation: true
