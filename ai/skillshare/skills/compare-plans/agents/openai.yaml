interface:
  display_name: "Compare Plans"
  short_description: "Conflict-first, severity-ordered plan comparison"
  default_prompt: "Use $compare-plans to compare the current plan with the supplied or clear sibling plan and return a conflict-first table of independently supported recommendations."
