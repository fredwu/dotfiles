interface:
  display_name: "Compare Worktree"
  short_description: "Compare another worktree for useful improvements"
  default_prompt: "Use $compare-worktree with exactly one target worktree path and return only actionable improvements for the current worktree."
policy:
  allow_implicit_invocation: false
