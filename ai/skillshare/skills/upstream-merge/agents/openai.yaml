interface:
  display_name: "Upstream Merge"
  short_description: "Merge upstream into main with careful conflicts"
  default_prompt: "Use $upstream-merge to merge `upstream` into `main`, preserving intentional customizations and incorporating compatible upstream improvements."
