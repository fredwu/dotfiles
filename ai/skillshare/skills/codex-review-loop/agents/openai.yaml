interface:
  display_name: "Codex Review Loop"
  short_description: "Iterate with bounded external Codex reviews"
  default_prompt: "Use $codex-review-loop to review and remediate the current change through bounded Codex rounds."
policy:
  allow_implicit_invocation: false
